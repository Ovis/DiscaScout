# Scraping Policy

## 1. 目的

DiscaScout は TSUTAYA DISCAS の検索結果を定期的に取得しますが、相手側サーバーへ不要な負荷をかけないことを最重要の運用要件とします。

この文書は、通常クロール、手動実行、リトライ、将来のアーティスト全作品取得を含むすべての DISCAS HTTP アクセスで維持する制約を定めます。

## 2. アクセス頻度

DISCAS への HTTP リクエストは並列化しません。

また、連続するリクエストの開始時刻を最低 **2 秒**空けます。

この制約はカテゴリクロールだけに実装せず、共通の `DiscasPageFetcher` で保証します。これにより、将来別のスクレイピング処理を追加した場合も同じ Fetcher を利用する限りアクセス制限を回避できない構造にします。

短時間で処理を完了させることより、相手サーバーへのアクセス頻度を抑えることを優先します。ページ数が増えた場合も並列化による高速化は行いません。

## 3. ジャンル取得

通常クロールは `New` と `Upcoming` を全ジャンルでそれぞれ一度だけ取得します。

J-POP、アニメ／ゲーム等をジャンル別に個別クロールすると同じカテゴリページへの追加アクセスが必要になるため、その方式は採用しません。

現在の DISCAS 検索結果 HTML には商品ごとの GA 用メタデータとして次の値が含まれています。

- `genre_large`
- `genre_mid`
- `genre_min`
- `titleid`

DiscaScout は `titleid` を商品識別子と対応付け、追加 HTTP リクエストなしでジャンルを取得します。

保存対象は次のとおりです。

- `GenreLarge` — 大ジャンル。例: `J-POP`, `アニメ／ゲーム`, `邦画サントラ`
- `GenreMiddle` — 中ジャンル。値がない場合は null
- `GenreSmall` — 小ジャンル。値がない場合は null

2026-08-29 に取得した Upcoming 821 件では、全商品について titleID とジャンルメタデータの対応を確認しています。

ジャンルはユーザーが主要な対象を絞り込むために必要な情報なので、商品に対応する大ジャンルを解析できない場合は欠損値のまま保存せず、そのページの解析失敗として扱います。

## 4. 追加ページ取得の原則

検索結果 HTML だけで取得できる情報のために商品詳細ページを追加取得しません。

特にジャンルは検索結果自身に埋め込まれているため、ジャンル取得を目的とした detail page fetch は禁止します。

将来新たなメタデータが必要になった場合も、まず検索結果 HTML、埋め込みデータ、既存内部データから取得できないか確認し、全商品に対する追加リクエストは最後の手段とします。

## 5. リトライ

一時的な HTTP エラーに対するリトライを将来実装する場合も、共通 Fetcher の最低 2 秒間隔と非並列制約を維持します。

同じ失敗要求を短時間に連打しません。カテゴリ単位の再試行も、通常の実行計画で定めた時間を空けて行います。
